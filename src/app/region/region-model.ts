import {TagList} from '../tags/tags-model';
import {SafeHtml} from '@angular/platform-browser';

export class Region {
    id: string;
    title: string;
    subtitle: string;
    alias: string;
    content: string;
}
